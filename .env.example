# Email signup endpoint (Klaviyo / Mailchimp / custom).
# The signup form POSTs { "email": "..." } as JSON to this URL.
# Leave unset in dev; set before launch or submissions are not stored.
PUBLIC_SUBSCRIBE_ENDPOINT=
